{
  "name": "kast-configurator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate:cabinets": "node scripts/generate-cabinet-catalog.mjs",
    "predev": "npm run generate:cabinets",
    "dev": "vite",
    "prebuild": "npm run generate:cabinets",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.154.0"
  },
  "devDependencies": {
    "@types/three": "^0.154.0",
    "vite": "^5.0.0",
    "typescript": "^5.2.0"
  }
}
